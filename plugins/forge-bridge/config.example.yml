backend:
  url: "https://launcher.example.invalid"
  timeoutMs: 5000
  retries: 2
server:
  id: "forge-main"
  projectId: "default"
  profileId: "forge"
  channel: "stable"
security:
  failMode: "closed"
  requireLauncherSession: true
  requireIntegrity: true
  identityFile: "config/neverlauncher-forge-bridge/node-identity.properties"
heartbeat:
  intervalSeconds: 30

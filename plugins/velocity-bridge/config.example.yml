backend:
  url: "https://launcher.example.com"
  timeoutMs: 5000
  retries: 2
server:
  id: "velocity-main"
identity:
  file: "node-identity.properties"
security:
  failMode: "closed"
  requireLauncherSession: true
  requireIntegrity: true
profile:
  projectId: "default"
  profileId: "vanilla"
  channel: "stable"
